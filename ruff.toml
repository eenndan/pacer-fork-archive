# Ruff config for pacer's Python: binding generators + notebook tooling.
# Generated bindings and the conda env are excluded. Run: ruff check / ruff format
line-length = 100
target-version = "py311"

extend-exclude = [
    ".pixi",
    "build",
    "3rdparty",
    "bindings/*/*/__init__.pyi",
    # imgui bindings are gated off and the generator is vendored litgen tooling
    "bindings/imgui/generate-bindings.py",
]

[lint]
select = ["E", "F", "I", "UP", "B"]
ignore = [
    "E501",  # line length handled by the formatter
    "E402",  # notebooks legitimately import after path setup
]

[lint.per-file-ignores]
# notebooks: scratch imports / redefs / cross-cell names / compact zips are fine
"*.ipynb" = ["F401", "F811", "F821", "F841", "E402", "B905"]
